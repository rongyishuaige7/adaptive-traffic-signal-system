; Keep the real values in an untracked local configuration or environment.
[local_secrets]
build_flags =
  -DWIFI_SSID=\"YOUR_WIFI_SSID\"
  -DWIFI_PASS=\"YOUR_WIFI_PASSWORD\"
  -DPC_HOST=\"YOUR_BACKEND_LAN_ADDRESS\"
  -DPC_PORT=8000
